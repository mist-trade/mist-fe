# Contributing — mist-fe

本仓文档遵循统一规范，canonical 入口：

- **主规范**：[`mist/docs/governance/documentation-guide.md`](../mist/docs/governance/documentation-guide.md)（相对路径以 workspace 根 `mist/` 计；或直接打开 `mist` 仓该文件）
- **模板与工具**：[`mist/docs/readme-governance/`](../mist/docs/readme-governance/)（骨架、同步脚本、清单）

## 本仓特殊注意

- 图表截图涉及 ECharts 渲染时，固定 `lightweight-charts` 版本并在 PR 描述中附复现步骤。
- 远端 About 的 `topics` 需包含 `nextjs` / `visualization` 等前端关键词（见主规范 §6.2）。

改动 `README.md` / `README.zh-CN.md` 前请按主规范 §9 自检，双语互跳与 Badge 顺序需与模板一致。
